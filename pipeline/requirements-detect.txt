# --- Detection pipeline (runs NATIVELY on the host to use the GTX 1650 GPU) ---
# Installed in a host venv, NOT inside Docker. See README "Run the detection pipeline".
#
# torch is installed separately with the CUDA build so the GTX 1650 is used:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# (CPU-only also works, just slower — ultralytics falls back automatically.)

ultralytics==8.3.58        # YOLOv8 + built-in ByteTrack tracker
lap>=0.5.12                # linear assignment, required by ByteTrack
opencv-python==4.10.0.84
numpy==2.1.3
requests==2.32.3           # POST events to API + Gemini VLM REST calls
python-dotenv==1.0.1
# VLM (staff/zone validation) uses the Generative Language REST API via `requests`
# (no SDK dependency). Set GEMINI_API_KEY in .env to enable; else heuristics only.
