# Detection microservice deps (CPU). torch CPU is installed separately in the
# Dockerfile from the pytorch CPU index to keep the image smaller.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
requests==2.32.3
redis==5.2.1
ultralytics==8.3.58
numpy==2.1.3
lap>=0.5.12
